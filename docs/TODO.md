┌─────────────────────────────────────────────────────────────┐
│                   File Organizer · 待实现功能                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  □ 收藏路径                                                  │
│     常用路径可以收藏，归档时快速选择，不用每次手动输入。          │
│                                                             │
│  □ 撤回操作                                                  │
│     移动文件/文件夹后可以一键撤回，恢复到原始位置。              │
│     思路：移动前记录 (源路径, 目标路径)，撤回时反向移动。        │
│                                                             │
│  □ 操作UI                                                    │
│     纯命令行交互可以更友好：                                   │
│     - 彩色高亮                                               │
│     - 编号快捷键直接跳转                                      │
│     - 分页展示                                               │
│                                                             │
│  □ 输入识别（双引号路径）                                      │
│     路径中包含空格或特殊字符时，用户会用引号包裹。               │
│     输入时自动去除首尾引号。                                   │
│                                                             │
│  □ 初始化程序                                                │
│     首次运行时的引导流程：                                     │
│     - 设置来源目录（D:\\Downloads 等）                        │
│     - 配置 AI Draw 等分类路径                                  │
│     - 语言选择                                                │
│                                                             │
│  □ 最近常用路径                                              │
│     记录最近使用过的目标路径，归档时可以直接选，不用重新输入。    │
│     可以保存在 分类配置.json 的 settings 里。                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
